


void onMainMenuCreated(CRules@ this, CContextMenu@ menu)
{
   // CBitStream params;
    //params.write_u8(1);
    //Menu::addContextItemWithParams(Menu::addContextMenu(menu, "testo"),"testoo","CustomMenuButtons.as", "helloWorld",params);
}

void helloWorld(CBitStream@ params)
{
    print("no u");
}